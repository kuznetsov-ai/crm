version: '3.9'

# Studio CRM stack.
# Only the public nginx port is bound to the host (default :3500).
# Postgres + Redis + backend stay on the internal docker network.
# To run alongside other compose stacks, use `docker compose -p studio-crm up -d`.

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-studio_crm}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_HOST: db
      REDIS_URL: redis://redis:6379/0
      DJANGO_SETTINGS_MODULE: config.settings.prod
    expose:
      - "8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - media_data:/app/media
    command: >
      sh -c "python manage.py migrate --no-input &&
             python manage.py seed_demo &&
             daphne -b 0.0.0.0 -p 8000 config.asgi:application"

  nginx:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    restart: unless-stopped
    ports:
      - "${HOST_HTTP_PORT:-3500}:80"
    volumes:
      - media_data:/app/media
    depends_on:
      - backend

volumes:
  postgres_data:
  media_data:
